import { userEntityMock } from 'src/user/__mocks__/user.mock';
import { UpdateSubcomDTO } from '../dtos/update-subcom.dto';

export const updateSubcomMock: UpdateSubcomDTO = {
  userId: userEntityMock.id,
};
