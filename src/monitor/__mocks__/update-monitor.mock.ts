import { turmaMock } from '../../turma/__mocks__/turma.mock';
import { UpdateMonitorDTO } from '../dtos/update-monitor.dto';

export const updateMonitorMock: UpdateMonitorDTO = {
  userId: turmaMock.id,
};
