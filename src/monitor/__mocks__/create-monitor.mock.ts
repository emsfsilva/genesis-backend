import { turmaMock } from '../../turma/__mocks__/turma.mock';
import { CreateMonitorDTO } from '../dtos/create-monitor.dto';

export const createMonitorMock: CreateMonitorDTO = {
  userId: turmaMock.id,
};
